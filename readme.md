# dotfiles

## install

```bash
$ git clone https://github.com/harryparkdotio/dotfiles.git ~/.dotfiles
$ cd ~/.dotfiles
$ script/bootstrap
$ exec zsh # reload zsh to apply changes
```

#### on a new machine

```bash
$ script/coldstart
```
